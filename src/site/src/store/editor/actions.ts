import { ActionTree } from 'vuex';
import { StateInterface } from '../index';
import { EditorInterface } from './state';

const actions: ActionTree<EditorInterface, StateInterface> = 
{
};

export default actions;
